http {
    # Настройка upstream для балансировки нагрузки
    upstream backend_servers {
        server backend1.example.com;
        server backend2.example.com;
        server backend3.example.com;
    }

    # Rate Limiting: 10 запросов в минуту на один IP-адрес клиента
    limit_req_zone $binary_remote_addr zone=per_client:10m rate=10r/m;

    server {
        listen 80;

        # При превышении лимита — HTTP 429 (Too Many Requests)
        limit_req_status 429;

        location / {
            limit_req zone=per_client burst=1 nodelay;

            proxy_pass http://backend_servers;
        }
    }
}
